# NamedCount

**Namespace:** `Anjal.Store`

A label and a count, for the dashboard's bar charts.

## Members

- **Count** *(property)* - How many.
- **Name** *(property)* - What is being counted: a folder, a category, a sender.
- **Slot** *(property)* - Colour slot when the name is a category; otherwise .
