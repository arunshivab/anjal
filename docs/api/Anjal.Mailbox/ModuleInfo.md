# ModuleInfo

**Namespace:** `Anjal.Mailbox`

Metadata for the Anjal.Mailbox module. The module stores delivered mail in per-tenant, per-user Maildir directories and indexes it in the store for listing and retrieval.

## Members

- **Name** *(property)* - The module name.
- **Version** *(property)* - The semantic version of this module.
