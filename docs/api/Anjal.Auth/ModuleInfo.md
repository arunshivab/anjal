# ModuleInfo

**Namespace:** `Anjal.Auth`

Inbound mail authentication. Implements: SPF verification per RFC 7208 DKIM signature verification per RFC 6376 DMARC policy evaluation per RFC 7489 Authentication-Results header formatting per RFC 8601 All DNS lookups go through . Zero external NuGet dependencies; verification uses the BCL's System.Security.Cryptography.RSA.

## Members

- **Name** *(field)* - Module name as it appears in logs and diagnostics.
