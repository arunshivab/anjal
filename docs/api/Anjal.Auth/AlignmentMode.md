# AlignmentMode

**Namespace:** `Anjal.Auth`

DMARC alignment mode per RFC 7489 section 3.1.

## Members

- **Relaxed** *(field)* - Organizational domain match is sufficient (default).
- **Strict** *(field)* - Full domain match required.
