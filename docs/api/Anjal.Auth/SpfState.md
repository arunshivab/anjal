# SpfState

**Namespace:** `Anjal.Auth.SpfVerifier`

## Members

- **IncrementVoidLookups** *(method)* - RFC 7208 section 4.6.4: lookups that return no answer are capped separately, because a record built from names that do not resolve costs the evaluator DNS round trips while never matching anything.
