# WebhookDispatchResult

**Namespace:** `Anjal.Routing`

Outcome of a webhook delivery attempt.

## Members

- **Completed** *(property)* - Whether the call completed without throwing.
- **ErrorMessage** *(property)* - Error text if is .
- **StatusCode** *(property)* - The HTTP status code returned, or 0 if no response.
